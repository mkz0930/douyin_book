我将优化 `src/search_client.py` 以确保搜索结果的准确性，并解决“百年孤独”的误导问题。

1.  **修改 `src/search_client.py`**：
    *   **优化搜索逻辑**：增加重试机制（Retry），如果默认搜索失败，自动切换到 `lite` 后端（纯 HTML 模式），这能显著提高 DuckDuckGo 的稳定性。
    *   **移除误导性测试代码**：注释掉文件底部关于“百年孤独”的测试代码，避免产生混淆的日志。
    *   **增强日志**：增加调试信息，显示搜索到的结果数量及具体的失败原因。

2.  **验证**：
    *   运行测试命令专门搜索“毛泽东选集”，确认能返回有效的书籍简介和评价信息。