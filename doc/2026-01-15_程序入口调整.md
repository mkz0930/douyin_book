# 程序入口调整记录 (2026-01-15)

## 1. 变更内容
将主程序入口文件 `src/main.py` 移动到了项目根目录 `d:\learn\douyin_book\main.py`。

## 2. 变更原因
用户反馈希望更方便地调用主程序，将其置于根目录可以减少路径输入，符合 Python 项目的常见结构。

## 3. 技术调整细节
- **文件移动**: 从 `src/` 移至根目录。
- **路径修正**:
  - `sys.path`: 修改了 `sys.path.append` 逻辑，确保能正确导入 `src` 包下的模块。
    - 原代码: `sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))`
    - 新代码: `sys.path.append(os.path.dirname(os.path.abspath(__file__)))`
  - `base_dir`: 修正了项目根目录的获取方式。
    - 原代码: `base_dir = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`
    - 新代码: `base_dir = os.path.dirname(os.path.abspath(__file__))`

## 4. 验证结果
执行 `python main.py --help` 成功，参数解析和模块导入正常。
